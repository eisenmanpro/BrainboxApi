package com.afrithecus.brainbox.api.learning.model

/** Content scope (doc 03 §1.2). */
enum class LearningScope { GLOBAL, SCHOOL, SCHOOL_GRADE_CLASS }

/** Content block shapes (doc 03 content model). */
enum class ContentType { NOTES, VIDEO, QUIZ, DIAGRAM, DOCUMENT }

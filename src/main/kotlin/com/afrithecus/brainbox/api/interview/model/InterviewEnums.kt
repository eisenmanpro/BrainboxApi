package com.afrithecus.brainbox.api.interview.model

/** Interview vocabulary; names match the Android enums exactly. */
enum class PracticeType { UNIVERSITY_INTERVIEW, JOB_INTERVIEW, LANGUAGE_SPEAKING, PRESENTATION_PRACTICE }

enum class InterviewMode { Q_A, SPEECH, MIXED }

enum class InterviewRubricType { NONE, STAR, SOAR, SHARE }

enum class InterviewStatus { IN_PROGRESS, COMPLETED, ABANDONED }
